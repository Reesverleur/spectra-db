# README

This page is generated from the repository root `README.md`.

When building on GitHub Pages, the workflow copies `README.md` → `docs/README.md` before running `mkdocs build`.

For local preview, run:

```bash
cp README.md docs/README.md
mkdocs serve
```
