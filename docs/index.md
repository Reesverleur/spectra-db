# Spectra DB

Local spectroscopic database + query API, with ingestion tools for NIST sources.

## Goals
- Canonical, editable normalized datasets
- Fast local queries via DuckDB
- Scrapers live in `tools/` and are not part of the package
