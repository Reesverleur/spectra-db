# Contributing

## Setup
- Create a virtualenv
- Install extras: `pip install -e ".[dev,docs,scrape]"`

## Style
- Ruff formatting/lint
- Every function must have a docstring
- Use `if __name__ == "__main__":` for demos in each file
